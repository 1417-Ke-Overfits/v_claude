# macOS
.DS_Store

# Dataset files (large raw data, provided separately — exceed GitHub's 100MB limit)
dataset/train/*.tsv
dataset/test/*.tsv
*.tsv

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.ipynb_checkpoints/

# Project venv & processed data
.venv/
data/processed/
data/interim/
*.parquet

# Large regenerable candidate/output TSVs
output/*.tsv
data/interim/

# Trained model binaries (regenerable via train_model.py)
models/xgb.json
models/lgbm.txt
